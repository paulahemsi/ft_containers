#include <iomanip>

namespace ft {

	template<class T, class Allocator = std::allocator<T> >
	class vector
	{
		private:
			/* data */
		public:
			vector(/* args */);
			~vector();
			void			push_back(T) {
			}
			void			pop_back(T) {
			}
			unsigned int	size(const &vector) {
				
			}
			void			insert(iteraror) {
				
			}
			iterator		find(T) {
				
			}
			T &operator[](unsigned int index) const {
				
			}
			// at()
			// max_size() (!?)
			// capacity()
			
	};
}

